{
  "name": "openshorts-publisher-heartbeat",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker cron that keeps the publishing host awake and alerts when it is not.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  }
}
